apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sm-legal-contract
  namespace: sm-platform
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "1m"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - sm-legal-contract.sm-platform.example.com
      secretName: sm-legal-contract-tls
  rules:
    - host: sm-legal-contract.sm-platform.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sm-legal-contract
                port:
                  number: 8300
